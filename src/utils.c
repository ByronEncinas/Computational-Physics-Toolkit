

/*// i want a function that frees memory, reasigning an array to a new size
void resize(float *array, int size, int new_size){

	if (size <= 0)
	{
	printf("[Invalid Parameter] `size` argument cannot be -0");
	}
        if (new_size <= 0)
        {
        printf("[Invalid Parameter] `new_size` argument cannot be -0");
        }


} */
